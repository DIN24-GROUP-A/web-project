<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - ExcavatorPro</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <div class="container">
        <div class="login-container text-center">
            <h2 class="mb-4">Login to ........</h2>
            
            <!-- Display success message if it exists -->
            {{#if message}}
                <h4 class="alert alert-success mt-4">{{message}}</h4>
            {{/if}}

            <form action="/auth/login" method="POST">
                <div class="mb-3">
                    <input type="email" name="email" class="form-control" placeholder="Email" required>
                </div>
                <div class="mb-3">
                    <input type="password" name="password" class="form-control" placeholder="Password" required>
                </div>
                <button type="submit" class="btn btn-custom mb-3">Login</button>
<p><a href="/login?message=Please+try+remembering+it!+%F0%9F%98%85" class="text-warning">Forgot password?</a></p>
                <p>Don't have an account? <a href="/register" class="text-warning">Register</a></p>
            </form>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
